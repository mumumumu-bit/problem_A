你现在暂停继续开发和跑新实验。

当前项目需要正式交接给另一位开发者继续完成。

本轮唯一任务不是优化算法，而是：

“冻结当前真实状态 + 核验实验事实 + 整理交接材料”。

不要继续修改算法。
不要继续跑100例。
不要写新的论文结果。
不要补造任何实验数字。

==================================================
一、首先解决一个非常重要的事实冲突
==================================================

目前我们看到的项目材料中存在明显冲突：

一份进度材料写：

“100个全量case一个都没跑，
目前所有实验都在15个代表case上。”

但当前论文/报告草稿中又出现：

“100例正式实验”
以及类似：

P1 N=2 平均加速比 1.51
P1 N=4 平均加速比 2.38
P2 N=2 平均加速比 1.65
P2 N=4 平均加速比 2.74

还有：

“100例 × P1/P2 × N=2/N=4，共400组”

这些信息不能仅靠聊天记录判断。

请以当前仓库中的真实文件为唯一依据核验。

==================================================
二、检查真实 Git 状态
==================================================

执行：

git status
git branch --show-current
git log -10 --oneline
git remote -v

记录：

- 当前 branch
- HEAD commit
- 未提交文件
- 未跟踪文件
- 本轮新增或修改了哪些文件

如果当前有未提交的重要结果：

暂时不要清理，不要 reset。

先记录。

==================================================
三、核验到底跑过哪些实验
==================================================

扫描整个项目中的：

results/
logs/
artifacts/
docs/
configs/

以及所有：

manifest.json
benchmark.csv
summary.csv
trials.jsonl
job.json
REPORT.md
comparison.csv
*.log

禁止根据 README 或论文文字推断。

请建立：

docs/EXPERIMENT_INVENTORY.md

逐个列出真实存在的实验。

至少包含：

run_name
source_commit
config
cases
num_cases
problem
cores
algorithm
time_budget
max_evaluations
seed
number_of_jobs
finished_jobs
CLI_verified_jobs
result_directory

==================================================
四、明确回答“100例到底有没有跑”
==================================================

必须根据真实结果文件回答。

分别统计：

Problem 1:
N=2 / 3 / 4 / 5

Problem 2:
N=2 / 3 / 4 / 5

Problem 3:
N=2 / 3 / 4 / 5

对于每个组合报告：

planned cases
actually completed cases
failed cases
missing cases

例如：

P1/N2: 15/100
P1/N4: 15/100

或：

P1/N2: 100/100

必须由文件逐条验证。

禁止因为论文中写了“100例”
就认为已经跑过。

==================================================
五、核验当前所有核心数字
==================================================

请核验以下目前出现过的数字：

P1 N4 speedup:
2.48?
2.38?
还是其他？

P2 N4:
2.75?
2.74?
还是其他？

平均改善率:
26.5%?
13.5%?
分别是什么定义？

VNS有效比例:
38/60?
25/36?
这些分别属于哪一轮实验？

15个代表case具体是哪15个？

这些数字必须标记来源：

【真实实验】
【旧版本实验】
【代表集实验】
【全量实验】
【论文草稿/占位数字】
【无法确认】

禁止把不同实验的数字混在一起。

==================================================
六、检查论文中的数字是否合法
==================================================

搜索所有：

*.tex
*.md
*.csv
*.json

特别检查当前论文中的：

“100例正式实验”
“平均加速比”
“平均改善率”
“large/small improvement”
“400组”

对于每个论文数字，找到对应原始结果文件。

建立：

docs/PAPER_RESULT_AUDIT.md

格式：

论文位置
论文数字
原始文件
计算方式
是否可复现
状态

状态只能是：

VERIFIED
OUTDATED
UNVERIFIED
PLACEHOLDER

如果不存在原始数据支撑：

必须标记 UNVERIFIED 或 PLACEHOLDER。

不要删除，先记录。

==================================================
七、整理当前真实算法状态
==================================================

建立：

docs/CURRENT_ALGORITHM.md

不要写计划，只写实际实现。

至少说明：

1. 当前 seed 有哪些；
2. 当前 coarsening 有哪些；
3. 当前 core assignment；
4. 当前 VNS 邻域 N1~N8；
5. acceptance rule；
6. cheap candidate ranking；
7. early stopping；
8. evaluator budget；
9. Memory heuristic 是否启用；
10. Cache heuristic 是否启用；
11. 是否有 SA；
12. 是否有 GA；
13. 是否有 Tabu；
14. 是否有 CP-SAT；
15. 是否存在任何全局优化模块。

对于没有实现的：

明确写：

NOT IMPLEMENTED

不要写“计划加入”混在当前算法里。

==================================================
八、核验当前15-case实验
==================================================

如果确实存在15个开发case，

请列出：

case id
规模
P1/P2/P3
N
Baseline
Multi-seed
VNS
最终Makespan
speedup
added_copy
runtime
official evaluator calls

重新计算：

P1 N2 mean speedup
P1 N4 mean speedup
P2 N2 mean speedup
P2 N4 mean speedup

以及：

Baseline → final improvement
Multi-seed → VNS improvement

不要复制旧报告里的数字，
直接从CSV重新算。

输出：

results/handoff_verified/development_summary.csv

==================================================
九、分析“效果不好”到底指什么
==================================================

不要简单写：

“还不够国赛水平”。

请用已有实验回答：

1. 哪些case改善明显？
2. 哪些case改善很小？
3. 是small还是large更差？
4. 是P1还是P2更差？
5. N=2还是N=4更差？
6. Multi-seed不够好，还是VNS不够好？
7. VNS有多少job完全没有进一步改善？
8. evaluator budget是否经常耗尽？
9. 大图的真实evaluation次数是否显著少于小图？

如果数据不足：

明确写“无法判断”。

==================================================
十、不要把主观“国赛门槛”当官方事实
==================================================

目前材料里出现过：

P1 N4 >= 3.0x
P2 N4 >= 3.0x
平均改善率 >= 35%
VNS有效比例 >= 80%

这些如果不是赛题官方规定，
只能标记为：

内部目标 / 经验目标

不能写成：

“国赛硬性要求”
或
“不达到就失去竞赛资格”。

请在交接报告中明确区分：

官方要求
vs
团队内部目标。

==================================================
十一、整理当前配置
==================================================

检查：

configs/default.yaml
configs/fast.yaml
configs/competition.yaml
以及其他新增config。

建立表格：

config
time_budget
max_evaluations
seed
seed_budget
VNS_budget
candidate_top_k
neighborhood参数
是否实际跑过
在哪个实验跑过

特别核实：

competition.yaml 是否真的使用过。

如果只是配置文件存在：

不能写“competition配置已经验证”。

==================================================
十二、测试
==================================================

运行：

python -m pytest -q

记录：

总测试数
通过
失败
跳过

同时确认：

code/
data/config.txt
data/case_*.json

是否存在我们的修改。

官方代码和数据不应被修改。

==================================================
十三、整理 Git 交接
==================================================

完成上述核验后：

创建：

HANDOFF_ROUND3.md

必须包含：

# 1. Git状态

branch
HEAD
working tree

# 2. 当前真实完成度

哪些完成
哪些未完成

# 3. 当前算法

真实已经实现的算法。

# 4. 实验库存

真实跑过哪些case/problem/core。

# 5. 当前可信结果

只放 VERIFIED 数字。

# 6. 不可信/待验证结果

包括论文草稿中的无源数字。

# 7. 当前问题

按优先级 P0/P1/P2。

# 8. 当前最值得继续的方向

最多列3项。

# 9. 下一位开发者的第一步

必须是具体可执行任务。

# 10. 可复现命令

给出当前最关键实验的真实命令。

==================================================
十四、不要做
==================================================

本轮禁止：

- 新增算法
- GA
- SA
- CP-SAT
- 调参
- 跑新的100例
- 修改competition配置
- 修改官方代码
- 修改官方数据
- 修改实验结果
- 为了让论文好看改数字
- 根据截图猜结果

==================================================
十五、完成后提交
==================================================

确认：

pytest通过
所有交接文档生成
实验原始结果未修改

然后：

git add .
git commit -m "docs: freeze round3 state and prepare handoff"

git push origin teammate/development-v3

不要merge到main。

==================================================
十六、最后只向我汇报
==================================================

【Git Branch】

【Commit】

【pytest】

【到底跑过多少case】

分别报告：
P1 N2/N3/N4/N5
P2 N2/N3/N4/N5
P3 N2/N3/N4/N5

【15个开发case是谁】

【可信的当前结果】

【论文中哪些数字没有实验支撑】

【当前算法真正实现到什么程度】

【当前最严重的3个问题】

【下一位开发者应该从哪一步继续】

不要再开始新的开发。